#include<stdio.h>
#include<conio.h> 
void main(void) 
{ 
       int number,increase=2,first=0,second=1,third,sum=1; 
       clrscr(); 
       prinrf("enter the number");
       scanf("%d",&number); 
       

       while(number>0) 
       { 
             third=first+second; 
             first=second;
             second=third; 
             sum=sum+third;
             increase=increase+1;
        } 
        printf("%d",sum); 
        getch();
}
