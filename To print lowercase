#include<stdio.h>
#include<conio.h>
void main(void) 
{
      char character,increase;
      clrscr();
      printf("enter the character");
      scanf("%c",&character);
      for(increase=97;increase<=122;increase++)
       {
              printf("%c",increase) 
              increase=increase+1;
          } 
          gerch(); 
}
