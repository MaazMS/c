#include<stdio.h>
#include<conio.h>
int fun(int a);
int main()
{
  int a=10;
  clrscr();
  a=fun(a);
  printf("\n %d ",a);
  getch();
  return 0;
}
int fun(int a)
{
     int no=10;
     no=no+a;
     return no;
}
/*    output 
20               */
