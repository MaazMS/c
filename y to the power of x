#include<stdio.h>
#include<conio.h
void main()
{ 
      int x,y,z=1,i=1;
      clrscr();
      printf("enter the number of x and y"); 
      scanf("%d%d",&x,&y);
      for(i=1;i<=y;i++)
      {
              z=z * x; 
      }
      printf("%d",z);
      getch();
 }     
